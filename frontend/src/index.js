import React from "react";
import ReactDOM from "react-dom/client";
import AppFivetab from "./AppFivetab";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <AppFivetab />
  </React.StrictMode>
);
